{
    "name": "synapse-local-backend",
    "version": "1.0.0",
    "description": "Local mock server for Synapse to avoid AWS Docker dependencies",
    "main": "server.js",
    "scripts": {
        "start": "node --env-file=.env server.js"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "groq-sdk": "^0.5.0"
    }
}