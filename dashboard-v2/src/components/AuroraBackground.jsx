// AuroraBackground replaced with static CSS grid — grid is now applied via body in index.css
// This component is kept as a no-op to avoid breaking imports
export default function AuroraBackground() {
    return null;
}
